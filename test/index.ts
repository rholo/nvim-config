import Demo from './Demo';
interface User {
  name: string;
  code: number;
  extras: string[]
}
const getUsr = ( user: User): number => {
  return user.name
}

method('rho')
getUser({name: 'lala'})

class MyClass {
  private _lala = '';
  constructor() {
    
  }
  get lala() {
    return this._lala;
  }
}

